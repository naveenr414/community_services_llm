{
  "name": "community-services",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "prestart": "cd frontend && npm install && npm run build",
    "start": "cd backend && python -m uvicorn app.all_endpoints:socket_app --reload"
  },
  "dependencies": {
    "remark-gfm": "^4.0.1"
  }
}
